version: '3.2'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=vitaliy
      - POSTGRES_PASSWORD=608752vet
    ports:
      - '5432:5432'
    volumes:
      - ./db/:/var/lib/postgresql/data
